#pragma once
#include <vector>
#include <string>

using namespace std;

class items{
private:
	int TotalItems;
	double total;

public:
	void setTotalItems(int a);
	int getTotalItems();
	void setTotal(double b);
	double getTotal();

};